create .env file consist of port=8080
NODE_MODE=developement
JWT_SECRET= 
DB_HOST=localhost
DB_USER=root
DB_PASS=  
DB_NAME=rail


and the use postman for api calling and in headers of postman write authorization: bearer token-detail
